# Prototype TODO

- [x] Replace cached itinerary with Vertex AI (Gemini 1.5 Flash) call via Vertex AI Python SDK (with cache fallback).
- [ ] Integrate Places API lookup with quota-safe batching layer.
- [ ] Persist confirmed itineraries to Firestore (Spark plan).
- [ ] Log booking intents to BigQuery sandbox dataset (`trip_planner.events`).
- [ ] Draft deck slides (problem, solution, architecture, roadmap).
- [ ] Record 3-min demo video.
